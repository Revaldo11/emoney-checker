abstract class AppRoutes {
  static const initial = '/';
  static const home = '/home';
  static const scan = '/scan';
  static const history = '/history'; // detail riwayat 1 kartu, arguments: cardUid
}
